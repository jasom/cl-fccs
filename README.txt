FantasyCraft character sheet manager

There are a lot of dependencies not currently in quicklisp, so it may be
easiest to use the docker image.

Note that there is a missing file (data/data.lisp) that must be generated
from the 2nd edition rulebook pdf using https://github.com/jasom/fccg
